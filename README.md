# TaskForge Backend

This is a simple Spring Boot backend for managing To-Do tasks.
It supports:
- Adding tasks
- Viewing tasks
- Updating tasks
- Deleting tasks

## How to run
1. Make sure Java 17 is installed
2. Run this command in the project folder:
